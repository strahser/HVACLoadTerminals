# Unit Test UT-7: T1.6 Core.Tests (xUnit project)

- Date: 2026-08-02T12:22:00Z
- Target: src/Core.Tests/ (new xUnit net48 test project for HVACLoadTerminals.Core)
- Test: 29 xUnit facts, executed via `dotnet test` (offline restore from NuGet cache)
- Result: PASS — passed 29, failed 0, skipped 0, total 29 (417 ms)

## Files created (5)

| File | Contents |
|------|----------|
| src/Core.Tests/HVACLoadTerminals.Core.Tests.csproj | SDK net48, xunit 2.5.3, Microsoft.NET.Test.Sdk 17.8.0, xunit.runner.visualstudio 2.5.3, ProjectReference -> Core |
| src/Core.Tests/GeometryTests.cs | Clipper2 offset (inward/outward/too-large), CleanPolygon, IsPointInPolygon, MmToUnits, PolygonArea |
| src/Core.Tests/RoomGeometryAnalyzerTests.cs | GetEdges, LongSide/ShortSide selection, Bottom/Top/Right/Left coordinate resolution, ResolveCoordinateSystem |
| src/Core.Tests/QuantityCalculatorTests.cs | ByCalculation ceil + cap, ByCount exact, ByStep (step 1 and step 2), TotalCapacity |
| src/Core.Tests/PlacementServiceTests.cs | Integration: count, wall offset, ByCount, LongSide edge index, FanCoil cooling mode, allowed-families filter, no-device warning |

## Key implementation semantics verified (test expectations matched to implementation)

1. `RoomGeometryAnalyzer.SelectPrimaryEdge` coordinate semantics (OBSERVED in source):
   - Bottom = edge with LARGEST average Y; Top = SMALLEST average Y; Right = LARGEST average X; Left = SMALLEST average X.
   - (Differs from the naive screen-coords guess in the task brief — tests assert the implemented behavior.)
2. `QuantityCalculator.CalculateCount` ByStep with stepCount=2: series 1,3,5,7,9,11 -> 11 (overshoot past 10); stepCount=1 -> 10.
3. `ClipperGeometryService.OffsetInward` 10x10 square offset 1 (Round joins): area ~= 63.1 (erosion by disk:
   100 - 40 + pi), asserted in (55, 100) — NOT the [64, 81] range suggested in the brief.
4. `TerminalPlacementService` distributes devices along the primary edge INCLUDING the endpoints;
   tests use `StartOffsetMm > 0` when asserting "all positions strictly inside" or
   "distance to nearest edge == wall offset" (endpoints would otherwise sit on corner projections).
5. Family names of exhaust devices kept distinct (E1 "Grille", E2 "Grille-Compact") so the
   allowed-families filter test is meaningful.

## Build/test evidence

```
"C:\Program Files\dotnet\dotnet.exe" test src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj --nologo -v m
  Восстановлен ...HVACLoadTerminals.Core.Tests.csproj (за 1,67 с).  [restore from cache, offline OK]
  HVACLoadTerminals.Core -> ...\Core\bin\Debug\net48\HVACLoadTerminals.Core.dll
  HVACLoadTerminals.Core.Tests -> ...\Core.Tests\bin\Debug\net48\HVACLoadTerminals.Core.Tests.dll
  Пройден!   : не пройдено     0, пройдено    29, пропущено     0, всего    29, длительность 417 ms.
```

(«Пройден!» = Passed; «не пройдено» = failed.)

## Notes
- Tests are the deliverable itself (T1.6 S1.6.1/S1.6.2/S1.6.3) — no isolated-then-deleted test applies here;
  the permanent suite doubles as the verification.
- No source files modified; zero regressions in Core (compiled as part of the test build).
