C# Revit  нужно сделать программу для расчета нормируемого термического сопротивления конструкции. Делаем базовый вариант, т.е. на вход методов подаются необходимые для расчета значения. у нас есть класс связанный с типом ограждающих конструкций EnclosureTypeOptions для него нужно добавить связь с типом конструкций описанных в СП 50, если типов не хватает, нужно добавить их в класс. к полям класса нужно добавить атрибут [Description("Описание")] в котором добавить описание связуемых полей. Нужно сделать на wpf  fodi reacui. пользователь выбирает тип здания  например из модели BuildingCategory, затем мы расчитываем ГСОП и определяем каждое нормативное значение для категорий. После этого мы отображаем таблицу с нормативными категориями. После этого мы через коллектор получаем для категории  из GetDirectShapeElements все уникальные конструкции сгруппированные по EnclosureType. в модели конструкций  есть поле NormativeTransferCoefficient  в котором мы отображаем нормативные конструкции. пользователь может изменить EnclosureType, для уточнения расчета с СП 50.  Пользователь должен иметь возможность ввести TransferCoefficient  в ручную или установив check box использовать нормативное значение принять необходимые значения из нормативного коэффициента. Установленные настройки должны сохраниться в json, если json пустой то выводятся настройки по умолчанию, если в json есть данные то они предопределены в форму

        [Description("Коэф. Теплопередачи")]
        [RevitParameter]
        public double TransferCoefficient { get; set; }
       [Description(" Нормируемый Коэф. Теплопередачи")]
        [RevitParameter]
        public double NormativeTransferCoefficient { get; set; } 
        public static IList<Element> GetDirectShapeElements(Document doc)
        {
            return new FilteredElementCollector(doc)
                .OfCategory(BuiltInCategory.OST_GenericModel)
                .Where(e => e.GetType() == typeof(Autodesk.Revit.DB.DirectShape))
                .ToList();
        }
class BuildingCategory(Enum):
	living = "Жилые, гостиницы и общежития"
	schools = "Дошкольные образовательные организации, общеобразовательные организации, медицинские организации и интернаты"
	public ='Общественные, кроме указанных выше, административные и бытовые, за исключением помещений с влажным или мокрым режимом'
	industrial ='Производственные с сухим и нормальным режимами'



 нужно определить GSOP По формуле Градусо-сутки отопительного периода, (°С · сут)/год, определяют по формуле
В соответствии с п. 5.2 СП 50.13330.2012 "Тепловая защита зданий", градусо-сутки отопительного периода (ГСОП), °С·сут/год, определяют по формуле:

ГСОП = (tв - tот) × zот
где, ГСОП - градусо-сутки отопительного периода.

tв - расчетная температура внутреннего воздуха здания, °С, принимаемая при расчете ограждающих конструкций групп зданий указанных в таблице 3: по поз.1 - по минимальным значениям оптимальной температуры соответствующих зданий по ГОСТ 30494 (в интервале 20-22 °С); по поз.2 - согласно классификации помещений и минимальных значений оптимальной температуры по ГОСТ 30494 (в интервале 16-21 °С); по поз.3 - по нормам проектирования соответствующих зданий.

tот - средняя температура наружного воздуха, °С, принимаемая по СП 131.13330.2020 для жилых и общественных зданий для периода со среднесуточной температурой наружного воздуха не более 8°С, а при проектировании дошкольных образовательных организаций, общеобразовательных организаций, медицинских организаций и домов-интернатов для престарелых не более 10°С.

zот - продолжительность, сут/год, отопительного периода, принимаемая по СП 131.13330.2020 для жилых и общественных зданий для периода со среднесуточной температурой наружного воздуха не более 8°С, а при проектировании дошкольных образовательных организаций, общеобразовательных организаций, медицинских организаций и домов-интернатов для престарелых не более 10°С.

    public static class EnclosureTypeOptions
    {
        public const string Wall = "Стена";
        public const string Curtain = "Витраж";
        public const string Window = "Окно";
        public const string Door = "Дверь";
        public const string Floor = "Пол";
        public const string Roof = "Кровля";
        
    }
BuildingCategory  нужно взять из СП 50 создать статический класс, в котором будут сохраняться коэффициенты a,b. для окон коэффициенты не определены поэтому нужно сохранять таблицу ГСОП для окон в разных условиях. 
Вот пример (на основе питона, но это не принципиально)
class BuildingCategory(Enum):
	living = "Жилые, гостиницы и общежития"
	schools = "Дошкольные образовательные организации, общеобразовательные организации, медицинские организации и интернаты"
	public ='Общественные, кроме указанных выше, административные и бытовые, за исключением помещений с влажным или мокрым режимом'
	industrial ='Производственные с сухим и нормальным режимами'
class StaticCoefficientStructures:
    def __init__(self):
        pass

    normal_window_gsop_base = [2000, 4000, 6000, 8000, 10000, 12000]  # только для МКД
    normal_thermal_coefficient_window_base = [0.49, 0.63, 0.73, 0.75, 0.77, 0.8]  # только для МКД
    check_compact_level_coefficient = {1: 1.1,
                                       2: 0.9,
                                       3: 0.54
                                       }
    def calculate_normative_coefficient(self) -> NormativeData:
        a = self._get_coefficient('a')
        b = self._get_coefficient('b')
        wall = a.wall * self.GSOP + b.wall
        door = wall * 0.55
        if a.window and b.window:
            window = a.window * self.GSOP + b.window
        else:
            window = np.interp(self.GSOP,
                               StaticCoefficientStructures.normal_window_gsop_base,
                               StaticCoefficientStructures.normal_thermal_coefficient_window_base
                               )
        floor = a.floor * self.GSOP + b.floor
        roof = a.roof * self.GSOP + b.roof
        skylight = a.skylight * self.GSOP + b.skylight
        normative_data = NormativeData(wall=round(wall, 2), door=round(door, 2),
                                       window=round(window, 2), floor=round(floor, 2),
                                       roof=round(roof, 2), skylight=round(skylight, 2))
        return normative_data
 
