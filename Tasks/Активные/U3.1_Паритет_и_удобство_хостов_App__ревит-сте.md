---
id: U3.1
приоритет: высокий
статус: in_progress
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-23T19:40:31
зависимости: нет
источник_запроса: план 2026-08-23_ui-usability-critique-fixes.md
---

# КАРТОЧКА U3.1 — Паритет и удобство хостов App ↔ ревит-стенд

## Цель
оба окна ведут себя одинаково; числа понятны инженеру.

## Описание (факт по коду)
(1) координаты X/Y в таблицах размещений и HTML-тултипе — в мм
  (конвертация `LengthUnitConverter`, сейчас футы); (2) колонка «RoomId» показывает
  «№. Имя» (сейчас внутр. Id); добавить «Уровень» в таблицу размещений; (3) k_ef цветом
  на плане (пороги <0.6 / 0.6–0.9 / >0.9) и в гриде; (4) тулбар стенда дополнить до
  паритета с App: правило вытяжки, v решётки, назначение, сохранить/загрузить проект,
  HTML; (5) LiveRecalc debounce ~300 мс (сейчас полный пересчёт всех комнат на каждый
  символ — комментарий «debounced» лжив); (6) числовые поля: валидация с сообщением
  вместо молчаливого Math.Max.

## Задание
presenter (debounce), MainViewModel, оба XAML, HtmlSceneExporter (мм).

## Критерии приёмки (проверяются механически)
1. сборка+тесты зелёные;
2. скриншоты обоих окон с одинаковыми тулбарами;
3. grep футов в формате ячеек отсутствует.

## Что проверяем
сценарий «правка Q → через 300 мс один пересчёт» (лог таймингов);
  стенд имеет те же контролы, что App.
- **Критерии приёмки**:
  1. сборка+тесты зелёные;
  2. скриншоты обоих окон с одинаковыми тулбарами;
  3. grep футов в формате ячеек отсутствует.

## Ожидаемые доказательства
скриншоты + лог + commit.

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\HVACLoadTerminals\Tasks\Отчёты\U3.1_Отчёт_2026-08-23_194031.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/U3.1.
