---
id: U1.3
приоритет: высокий
статус: in_progress
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-23T14:51:21
зависимости: нет
источник_запроса: план 2026-08-23_ui-usability-critique-fixes.md
---

# КАРТОЧКА U1.3 — HTML-превью: WebView2 вместо WebBrowser (баг-фикс)

## Цель
HTML-отчёт открывается и интерактивно работает офлайн из App и стенда.

## Описание (факт по коду)
выкинуть легаси `WebBrowser` из `src/App/Views/HtmlPreviewWindow.xaml`;
  общий WebView2-хост в Infrastructure (обобщение `src/Revit/Visualization/
  WebView2PreviewWindow.xaml(.cs)`): загрузка сцены через `HtmlSceneExporter.SaveToFile`
  или локальный сервер `HtmlPreviewServer`; мост postMessage уже есть в JS-шаблоне.
  Recompute передавать РЕАЛЬНЫЙ колбэк (presenter.Calculate → PlacementSceneSerializer,
  сейчас заглушка `() => sceneJson` в `OpenHtmlPreviewCommand.cs:35`). Обёртка
  `HtmlPreviewServer.Start()` try/catch → текст в статус, не краш. Русифицировать кнопки
  страницы (Apply/Cancel/Recompute → Применить/Отмена/Пересчитать) — правка шаблона
  `HtmlSceneExporter`.

## Входные данные
сцена после расчёта (`LastRawPlacements` + snapshot).

## Задание
src/App (Views/Commands/csproj), Infrastructure/Visualization
  (общий хост, HtmlSceneExporter), опционально ревит-стенд переключить на общий хост.

## Критерии приёмки (проверяются механически)
1. сборка EXITCODE=0;
2. скриншот работающего превью;
3. grep: `WebBrowser` в src/App отсутствует;
4. NuGet Microsoft.Web.WebView2 в App.csproj.

## Что проверяем
«🌐 HTML» после «РАССЧИТАТЬ» → окно со схемой: пан/зум/тултип работают;
  Recompute при смене правила количества меняет число точек на плане; офлайн (без сети)
  2D работает, 3D показывает внятное сообщение.
- **Критерии приёмки**:
  1. сборка EXITCODE=0;
  2. скриншот работающего превью;
  3. grep: `WebBrowser` в src/App отсутствует;
  4. NuGet Microsoft.Web.WebView2 в App.csproj.

## Ожидаемые доказательства
скриншоты + commit.

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — строго по точному пути из промпта (имя с меткой времени ЧЧММСС):
D:\Projects\HVACLoadTerminals\Tasks\Отчёты\U1.3_Отчёт_2026-08-23_145121.md.
Запись под другим именем = верификатор не найдёт отчёт и попытка сгорит.
Коммит plan/U1.3.
