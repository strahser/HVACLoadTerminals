---
id: U1.1
приоритет: высокий
статус: rejected
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-23T12:46:40
зависимости: нет
источник_запроса: план 2026-08-23_ui-usability-critique-fixes.md
---

# КАРТОЧКА U1.1 — Фильтр таблицы помещений по уровню (баг-фикс)

## Цель
выбор уровня в ComboBox фильтрует таблицу помещений в App и ревит-стенде.

## Описание (факт по коду)
назначить предикат `_roomsView.Filter = FilterVisible` при первом обращении
  к `RoomsView` (`src/App/ViewModels/MainViewModel.cs:33-50`); проверить ревит-хост
  `src/Revit/UI/SnapshotPlacementWindow.xaml.cs` — если он держит свой ICollectionView,
  применить тот же фикс; при смене уровня достаточно `RoomsView.Refresh()`.

## Входные данные
снимок с ≥2 уровнями (HvackFinal_v1.1.json из snapshots_raw).

## Задание
`src/App/ViewModels/MainViewModel.cs` (+ ревит-хост при необходимости).

## Критерии приёмки (проверяются механически)
1. сборка sln EXITCODE=0;
2. Core.Tests baseline 74/74;
3. скриншот App до/после выбора уровня приложен в отчёт карточки;
4. grep подтверждает наличие `.Filter =`.

## Что проверяем
выбор уровня → в гриде только его комнаты; «Все уровни» → все;
  «Применить назначение» продолжает работать по видимым.
- **Критерии приёмки**:
  1. сборка sln EXITCODE=0;
  2. Core.Tests baseline 74/74;
  3. скриншот App до/после выбора уровня приложен в отчёт карточки;
  4. grep подтверждает наличие `.Filter =`.

## Ожидаемые доказательства
скриншоты + лог тестов + commit.

## Гиперссылки
[план-родитель](2026-08-22_universal-terminal-placement.md).

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — Tasks\Отчёты\U1.1_Отчёт_<дата>.md; коммит plan/U1.1.
