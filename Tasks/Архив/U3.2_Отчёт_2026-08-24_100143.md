# U3.2 — Отчёт: Гигиена репозитория HVACLoadTerminals (2026-08-24 10:01)

**Карточка**: U3.2 плана `HVACLoadTerminals/_current/2026-08-23_ui-usability-critique-fixes.md`
**Исполнитель**: интерактивная сессия агента (ручная процедура по handoff
`.opencode/HANDOFF-2026-08-23-usability-plan.md` — раннер не задействован).
**Статус на момент отчёта**: работа выполнена, критерии приёмки подтверждены.

## Что сделано

Удалено легаси первого поколения (33 файла, всё было вне `HVACLoadTerminals.sln`):

1. **Корневой legacy-проект и его файлы** (ссылались только друг на друга через
   корневой `HVACLoadTerminals.csproj`, который в sln не входил):
   `HVACLoadTerminals.csproj`, `Commands.cs`, `DbUtility\` (3), `Models\` (3),
   `Utils\` (7), `ViewModels\` (2), `Views\` (6: 3 XAML + code-behind),
   `StaticData\SystemData.cs`, `Properties\AssemblyInfo.cs`, `App.config`,
   `lib\System.Data.SQLite.dll`.
2. **Мусор корня**: `test.sln`, `json_data_add_test.json`, `LiteDB.Studio.exe`
   (треканный бинарник!), `task agents.txt` (остаток промпта генерации №1 — сверх
   явного списка карточки, удалён по цели «репо не вводит в заблуждение»).
3. **Мёртвые дубликаты в src**: `src\App\ViewModels\SnapshotRows.cs`
   (классы `RoomRowViewModel`/`PlacementRowViewModel`; реальные строки —
   `Infrastructure\Presentation\Rows.cs`) + обязательный спутник
   `src\App\Services\PlacementProjectStore.cs` — единственный потребитель типов из
   SnapshotRows, сам нигде не использован (round-trip проекта делает presenter,
   U2.1/U2.2). Удалены вместе, иначе сборка App сломалась бы.
4. **README.md переписан под текущую архитектуру**: слои Core/Infrastructure/App/Revit,
   стенд «Снимок помещений» (фильтр уровней, чекбоксы выбора, режимы
   Auto/ByArea/ByFlow/Fixed, паттерны LongSide/ShortSide/SingleRule, LiveRecalc
   debounce, WebView2-превью, CRUD-каталог, round-trip проекта), лента Revit
   (8 фактических кнопок, включая «По снимку помещений» с маркером HLT|…|…|… и
   «Стенд расстановки»), ключевые сервисы ядра, команды сборки. Устаревшие разделы
   (демо-данные, футы, «33 теста», несуществующие сценарии) убраны.
5. `.gitignore`: пункт `LocalTestOutput/` уже присутствовал (занесён ранее,
   при U3.1-сессии); каталог не трекается — проверено `git ls-files LocalTestOutput`
   → пусто. Файл `$null` из списка карточки в репо уже отсутствовал.

## Доказательства

1. **Сборка sln EXITCODE=0**:
   `"C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" HVACLoadTerminals.sln /t:Build /p:Configuration=Debug /v:m /nologo`
   → собраны все 4 проекта (Core, Infrastructure, App, Revit), 0 ошибок;
   выведено `MSBUILD_EXITCODE=0`. Warning'и CS8602/CS8604 в Revit — существовавшие
   до карточки, не связаны с удалением.
2. **Core.Tests: пройдено 108 / всего 108, пропущено 0** (`dotnet test`,
   TEST_EXITCODE=0) — ≥ базы handoff (108) и конфига конвейера (74).
3. **`git ls-files` без *.exe и *.dll** → пусто (был `LiteDB.Studio.exe` +
   `lib\System.Data.SQLite.dll`). `git status --short` — только удаления D и M README.
4. **Ничего из удалённого не упомянуто в csproj/sln**: sln содержит только 4 проекта
   `src\*`; ссылок на легаси-типы из живого кода нет (`findstr /S` по
   SQLiteHelper|DSUtility|SpaceBoundary|SystemData|CollectorQuery|DeviceViewModel|OffsetDialog
   → 0 файлов); ссылок на `test.sln` в *.md/opencode.json → 0; XAML-ссылок на типы
   SnapshotRows → 0.
5. **Корень репо после чистки** (`git ls-files` вне src/Tasks/.opencode/.idea):
   только `.editorconfig`, `.gitattributes`, `.gitignore`, `HVACLoadTerminals.sln`,
   `LICENSE.txt`, `README.md`, `opencode.json`.

## Открытые вопросы

1. `.idea\` (файлы IDE Rider) остаётся треканным — вне объёма карточки; можно
   исключить отдельной правкой .gitignore, если владелец согласен.
2. Родительский план `2026-08-22_universal-terminal-placement.md`: карточка C4.1
   (прогон «По снимку помещений» в Revit 2024 + вердикт владельца) остаётся открытой —
   требует Revit-хоста и владельца.
3. Чекпоинт этапа U2/U3 в панели конвейера формально не одобрялся (работа шла по
   команде «продолжи», как и предыдущие карточки).
