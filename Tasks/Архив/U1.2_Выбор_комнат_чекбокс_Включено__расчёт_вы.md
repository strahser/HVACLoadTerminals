---
id: U1.2
приоритет: высокий
статус: verified
постановщик: план-раннер
исполнитель: subagent
дата: 2026-08-23T14:00:55
зависимости: нет
источник_запроса: план 2026-08-23_ui-usability-critique-fixes.md
---

# КАРТОЧКА U1.2 — Выбор комнат: чекбокс «Включено» + расчёт выбранных

## Цель
массовая расстановка по подмножеству комнат; индивидуальный режим из стенда.

## Описание (факт по коду)
`RoomRow += IsIncluded` (INPC, default true); колонка-чекбокс «☑» первой
  в обоих гридах (App MainWindow.xaml, SnapshotPlacementWindow.xaml);
  `SnapshotWorkspacePresenter.Calculate()` обрабатывает только `IsIncluded` (сейчас
  строка 179 `foreach (var row in Rooms)`); групповые операции: «включить уровень» /
  «только видимые»; счётчик выбранных в статусе; `IsIncluded` участвует в round-trip
  проекта (`ProjectDto.Rooms`).

## Входные данные
RoomRow/Rows.cs, presenter, оба XAML-грида.

## Задание
Infrastructure/Presentation (Rows.cs, SnapshotWorkspacePresenter.cs),
  src/App/MainWindow.xaml, src/Revit/UI/SnapshotPlacementWindow.xaml, Core.Tests.

## Критерии приёмки (проверяются механически)
1. юнит-тест presenter «Calculate respects IsIncluded»;
2. round-trip тест проекта с флагами;
3. Core.Tests ≥74 pass;
4. скриншот грида с чекбоксами.

## Что проверяем
включено 3 из N → приборы только в 3; все выключены → статус
  «Не выбрано ни одного помещения», расчёт не падает; сохранение/загрузка проекта
  сохраняет флаги.
- **Критерии приёмки**:
  1. юнит-тест presenter «Calculate respects IsIncluded»;
  2. round-trip тест проекта с флагами;
  3. Core.Tests ≥74 pass;
  4. скриншот грида с чекбоксами.

## Ожидаемые доказательства
тесты + скриншоты + commit.

## Границы (что НЕ делать)
- Не менять то, что не относится к карточке; не трогать чужие отчёты/планы.
- Не коммитить: .idea\, .opencode\, bin\obj, TestResults\, node_modules\.
- Не создавать субагентов; статус done ставит контролёр (план-раннер).

## Результат (куда положить артефакты)
Отчёт — Tasks\Отчёты\U1.2_Отчёт_<дата>.md; коммит plan/U1.2.
