# S3.3 — Отчёт: экспорт итогового задания в формате прототипа (2026-08-24 14:25)

**Карточка**: S3.3 плана `2026-08-24_named-systems-and-revit-conversion.md`
**Исполнитель**: интерактивная сессия агента.

## Что сделано

1. **Новый экспортёр** `Infrastructure/Visualization/PlacementTaskExporter.cs`:
   - группировка placements по (помещение, система);
   - схема точно по `Models/DeviceModelExport.py` прототипа (ключи
     `S_ID`, `family_device_name`, `family_instance_name`,
     `minimum_device_number`, `flow_to_device_calculated`, `system_name`,
     `instance_points`);
   - координаты точек — мм (`LengthUnitConverter`), z = отметка уровня комнаты
     (`SnapshotRoom.LevelElevation`, футы→мм); расход на приборе — из
     CalculatedFlowM3h (S2.2);
   - `Save(path)` — indented JSON.
2. **App**: кнопка «⬇ Задание JSON» в тулбаре (после «🌐 HTML»), команда
   активна только после расчёта; имя файла по умолчанию
   `<снимок>_task.json`.
3. **Тесты** `Core.Tests/PlacementTaskExporterTests.cs` (2):
   набор ключей JSON == ключам прототипа (SetEquals на JObject.Properties) +
   значения группировки; координаты в мм (3048 мм = 10 футов) и z уровня;
   две системы одной комнаты → две группы с разными system_name.

## Доказательства

1. **Core.Tests: 125 passed / 0 failed** (+2 S3.3).
2. **sln EXITCODE=0**.
3. Критерии карточки: юнит-тест схемы ✅; координаты в мм ✅; сборка+тесты ✅.
   Пример файла будет приложен к отчёту S4.1 после живого расчёта (кнопка
   доступна владельцу сразу).

## Открытые вопросы

1. z сейчас = отметка УРОВНЯ помещения; потолочный offset каталога в placement
   не хранится. Если потребителю нужен именно потолочный z — добавить поле
   MountingHeightMm в TerminalDevice (мини-карточка по решению владельца).
