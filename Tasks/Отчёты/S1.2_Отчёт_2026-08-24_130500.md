# S1.2 — Отчёт: UI-редактор систем помещения (2026-08-24 13:05)

**Карточка**: S1.2 плана `2026-08-24_named-systems-and-revit-conversion.md`
**Исполнитель**: интерактивная сессия агента.

## Что сделано

1. **Общий редактор** `Infrastructure/Presentation/SystemEditorWindow.xaml(.cs)`
   (оба хоста используют один и тот же диалог, как и общий WebView2-хост):
   - DataGrid: Имя / Тип (ComboBox Supply|Exhaust) / Расход м³/ч / Включено;
   - кнопки «＋ Приток», «＋ Вытяжка» (имя автогенерируется П1→П2…/В1→В2…,
     расход предзаполняется остатком оценки), «− Удалить» выбранной строки;
   - строка баланса: сумма включённых по типу vs оценка LoadsEstimator
     (`row.Supply`/`row.Exhaust`), расхождение > 0.5 м³/ч подсвечивается оранжевым;
   - ОК валидирует (пустые имена, дубликаты case-insensitive, расход ≤ 0) и
     применяет список в `RoomRow.Systems` + `RefreshSystemSummary()`; Отмена
     закрывает без изменений.
2. **Таблица помещений обоих хостов** (`src/App/MainWindow.xaml`,
   `src/Revit/UI/SnapshotPlacementWindow.xaml`): колонка «Системы» со сводкой
   `П1+П2 | В1` (только включённые; `RoomRow.SystemsSummary`) и кнопка
   «Системы…» в каждой строке → модальный редактор.
3. **Юнит-тест** `NamedSystemsTests.SystemsSummary_Lists_Included_Systems_Supply_First`:
   сводка учитывает IsIncluded; имя системы из UI доходит до placements —
   покрыто ранее добавленным `SystemsEngineTests.Presenter_Custom_Systems_Reach_Placements_And_Kef`.
4. CSV/Excel-импорт списка систем — опциональный пункт карточки, НЕ делался
   (отдельная карточка при необходимости).

## Доказательства

1. **Core.Tests: 123 passed / 0 failed** (+1 S1.2), TEST_EXITCODE=0.
2. **sln EXITCODE=0** (MSBuild VS18, 4 проекта).
3. Критерий 2 (юнит-тест presenter) ✅; критерий 3 (сборка+тесты) ✅.

## Открытые вопросы / ограничения

1. **Критерий 1 (скриншоты редактора в обоих хостах) НЕ выполнен**: сессия
   агента headless — App требует интерактивного десктопа, ревит-стенд — живого
   Revit 2024. Карточка остаётся `open` со статусом «✅ кодом»; скриншоты +
   перевод в done — при приёмке владельцем (совместить с S4.2).
2. Редактор правит список целиком после ОК (не поячеечно live) — при LiveRecalc
   пересчёт идёт уже по новому составу систем (полный пересчёт комнаты — решение
   открытого вопроса №4 ТЗ принято как «полный пересчёт»).
