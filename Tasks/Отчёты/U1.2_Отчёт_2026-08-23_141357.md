# U1.2 — Отчёт (попытка 2 / повторная верификация): выбор комнат — чекбокс «Включено» + расчёт выбранных

- **Дата**: 2026-08-23
- **Статус**: выполнено (код внесён коммитом `d92addb`, вердикт контролёра PASS `13b6ff4`); настоящая попытка — повторная механическая верификация критериев приёмки перед сдачей.
- **Карточка**: `Tasks/Активные/U1.2_Выбор_комнат_чекбокс_Включено__расчёт_вы.md` (перенесена в архив: `Tasks/Архив/U1.2_Выбор_комнат_чекбокс_Включено__расчёт_вы.md`)

## Решения (grill)

- Блокирующих неясностей нет: постановка однозначна, все «как делать» уже отвечены кодом и предыдущим проходом.
- Сверил состояние с фактом: карточка закрыта (вердикт `Tasks/Отчёты/U1.2_Вердикт_контролёра_2026-08-23_144905.md`, PASS), активная карточка в `Tasks/Активные/` — U3.1, `runner_state.json` указывает на U3.1.
- ASSUMPTION: перезапуск раннера по уже закрытой карточке — орфан; повторная попытка ничего не меняет в коде, только пере-верифицирует критерии и фиксирует факт отчётом (изменений в коде нет и быть не должно).

## Что было не так

- Код уже реализован и закоммичен (попытка 1, коммит `d92addb`); вердикт контролёра — PASS.
- При повторной механической проверке обнаружен ПРЕ-СУЩЕСТВУЮЩИЙ обрыв полной сборки решения вне скоупа U1.2: `src/Infrastructure/Visualization/WebView2PreviewWindow.xaml(.cs)` (WIP карточки U1.3) не компилируется — в `HVACLoadTerminals.Infrastructure.csproj` и `HVACLoadTerminals.App.csproj` подключён пакет `Microsoft.Web.WebView2` (Core), но НЕ `Microsoft.Web.WebView2.Wpf`, из которого XAML-окно тянет `wv2:WebView2` → поля `WebView`/`StatusText`/`InitializeComponent` не генерируются → CS0103 ×22. Не трогал (карточка U1.3, не U1.2).

## Что сделано

1. **Сверил реализацию U1.2 по коду** (проверено в текущем рабочем дереве, без правок):
   - `src/Infrastructure/Presentation/Rows.cs:16–22` — `RoomRow.IsIncluded` (INPC, default `true`).
   - `src/Infrastructure/Presentation/SnapshotWorkspacePresenter.cs` — `Calculate()` берёт только `IsIncluded` (строки 349, 366–369); пустой выбор → статус «Не выбрано ни одного помещения», `IsCalculation=true`, не падает (350–364); счётчик «Выбрано N из M» (641); групповые операции `CountIncluded/SetIncluded/IncludeLevel/IncludeOnlyVisible` (297–317).
   - `src/App/MainWindow.xaml:268–269` и `src/Revit/UI/SnapshotPlacementWindow.xaml:86–87` — колонка-чекбокс «☑» первой в обоих гридах; кнопки «☑ Включить уровень»/«☐ Только видимые» (MainWindow.xaml:183,187; команды `MainViewModel.cs:191–232`).
   - Round-trip проекта: `ProjectDto.Rooms` сериализует `RoomRow` целиком → флаги сохраняются.
2. **Повторная механическая проверка критериев приёмки** (см. «Доказательства»).
3. **Отчёт** `Tasks/Отчёты/U1.2_Отчёт_2026-08-23_141357.md`.

## Доказательства (проверка критериев приёмки)

| # | Критерий | Результат |
|---|----------|-----------|
| 1 | юнит-тест presenter «Calculate respects IsIncluded» | ✅ `SnapshotWorkspacePresenterTests.Calculate_Respects_IsIncluded` (src/Core.Tests/SnapshotWorkspacePresenterTests.cs:65): выключена комната «b» → все `LastRawPlacements` только в «a», статус содержит «Выбрано 1 из 2» |
| 2 | round-trip тест проекта с флагами | ✅ `Project_RoundTrip_Preserves_IsIncluded_Flags` (…:97): SaveProject→LoadProject сохраняет `[true,false]`; доп.: `Calculate_With_NoneSelected_Returns_Status_And_DoesNotThrow` (…:80), `IncludeLevel_And_IncludeOnlyVisible_Update_Selection` (…:113) |
| 3 | Core.Tests ≥74 pass | ✅ `dotnet test src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj` → **«не пройдено 0, пройдено 108, пропущено 0, всего 108, длительность 2 s»** (база 74; 108 включает все поздние карточки) |
| 4 | скриншот грида с чекбоксами | ✅ артефакт на месте: `Tasks/Отчёты/U1.2_артефакты/U1.2_grid_checkboxes.png` |

Дополнительно:
- `dotnet build src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj` → **Ошибок: 0** (Core + Infrastructure собираются; только предупреждения MSB3026 о файловой блокировке testhost-процессами параллельного агента — транзиентные).
- Полная `dotnet build HVACLoadTerminals.sln` → **22 ошибки CS0103** только в `WebView2PreviewWindow.xaml.cs` (U1.3 WIP, см. «Что было не так» и «Открытые вопросы»). На момент исходного прохода U1.2 (коммит `d92addb`) сборка решения была 0 ошибок; обрыв внесён позже карточкой U1.3 и к U1.2 отношения не имеет.

## ASSUMPTION

- ASSUMPTION: повторный запуск раннера по закрытой карточке — орфан конвейера; корректное действие — повторная верификация без изменения кода. Никакие файлы скоупа U1.2 не менялись.
- ASSUMPTION: коммит настоящей попытки содержит только отчёт (не `git add -A`): в рабочем дереве присутствует чужой неотслеживаемый файл параллельного агента `Tasks/Отчёты/U1.3_Отчёт_2026-08-23_153619.md` и `.opencode/permissions.json` — их не трогаю.

## Открытые вопросы

1. **Сборка полного решения сломана вне U1.2**: `WebView2PreviewWindow.xaml` требует сборку `Microsoft.Web.WebView2.Wpf`, а в `HVACLoadTerminals.Infrastructure.csproj`/`App.csproj` подключён только `Microsoft.Web.WebView2` (Core). Рекомендация: в карточке U1.3 добавить `<PackageReference Include="Microsoft.Web.WebView2.Wpf" />` (или удалить WPF-окно из Infrastructure). На момент проверки признаков, что это сделано, нет.
2. (из исходного отчёта U1.2) `Levels` пересоздаётся через `Clear()` на каждом `StateChanged` → программно установленный `SelectedLevel` сбрасывается WPF-биндингом в «Все уровни»; пользовательский сценарий не затронут — отдельная карточка при необходимости.

## Как проверить

1. `dotnet test src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj` → 108/108.
2. `src/Core.Tests/SnapshotWorkspacePresenterTests.cs` — критерии 1–2.
3. Открыть `Tasks/Отчёты/U1.2_артефакты/U1.2_grid_checkboxes.png` — колонка «☑» первая, статус «Выбрано 37 из 40 · Размещение: 620 приборов…».
4. В App (MainWindow.xaml) или ревит-стенде (SnapshotPlacementWindow.xaml): снять чекбоксы части комнат → «▶ РАССЧИТАТЬ» → приборы только в выбранных; все выключить → статус «Не выбрано ни одного помещения», без падения.

## Коммит

Настоящая попытка: коммит `plan/U1.2` с настоящим отчётом (повторная верификация). Исходная реализация: коммит `d92addb`.