# U1.2 — Отчёт: выбор комнат — чекбокс «Включено» + расчёт выбранных

- **Дата**: 2026-08-23 (повторная верификация по повторному диспатчу раннера)
- **Статус**: выполнено (карточка уже закрыта PASS 2026-08-23_144905)
- **Карточка**: `Tasks/Архив/U1.2_Выбор_комнат_чекбокс_Включено__расчёт_вы.md`

## Ситуация

Раннер повторно выдал карточку U1.2 (попытка 1, фаза executing), однако карточка
уже выполнена ранее: коммиты `d92addb` и `40509da` (plan/U1.2), вердикт контролёра
`U1.2_Вердикт_контролёра_2026-08-23_144905.md` — **PASS**. Первичный отчёт:
`Tasks/Отчёты/U1.2_Отчёт_2026-08-23.md`. Повторное исполнение не требуется —
изменения уже в коде и зафиксированы.

## Что было не так

- Повторный диспатч раннера на уже закрытую (verified) карточку — стейт-голодание
  конвейера. Код карточки в репозитории присутствует и работает.

## Что сделано (повторная верификация критериев, без правок кода)

- `src/Infrastructure/Presentation/Rows.cs` — `RoomRow.IsIncluded` (INPC, default true).
- `src/Infrastructure/Presentation/SnapshotWorkspacePresenter.cs` — `Calculate()` только
  по включённым комнатам, пустой выбор → «Не выбрано ни одного помещения» без падения,
  счётчик выбранных, групповые `SetIncluded`/`IncludeLevel`/`IncludeOnlyVisible`.
- `src/App/MainWindow.xaml` — колонка-чекбокс «☑» первой + кнопки «☑ Включить уровень» /
  «☐ Только видимые».
- `src/Revit/UI/SnapshotPlacementWindow.xaml` — та же колонка «☑» первой.
- `src/Core.Tests/SnapshotWorkspacePresenterTests.cs` — тесты `Calculate_Respects_IsIncluded`,
  `Project_RoundTrip_Preserves_IsIncluded_Flags`, `Calculate_With_NoneSelected_...`,
  `IncludeLevel_And_IncludeOnlyVisible_Update_Selection`.

## Доказательства (повторный прогон, текущий код)

| # | Критерий | Результат |
|---|----------|-----------|
| 1 | юнит-тест presenter «Calculate respects IsIncluded» | ✅ `Calculate_Respects_IsIncluded` (SnapshotWorkspacePresenterTests.cs:65) |
| 2 | round-trip тест проекта с флагами | ✅ `Project_RoundTrip_Preserves_IsIncluded_Flags` (SnapshotWorkspacePresenterTests.cs:97) |
| 3 | Core.Tests ≥74 pass | ✅ **108/108** (база 74): `Пройден! : не пройдено 0, пройдено 108, пропущено 0, всего 108` |
| 4 | скриншот грида с чекбоксами | ✅ `Tasks/Отчёты/U1.2_артефакты/U1.2_grid_checkboxes.png` (300 127 байт) |

Сборка: `dotnet build src/Core.Tests/HVACLoadTerminals.Core.Tests.csproj` → EXIT 0.

## ASSUMPTION

- ASSUMPTION: повторный диспатч — стейт-ошибка раннера (карточка в Архиве со статусом
  verified, вердикт PASS), поэтому правок кода не делал, только перепроверил критерии
  и зафиксировал отчёт.

## Открытые вопросы

- Отсутствуют (карточка закрыта). Известное наблюдение из первичного отчёта (вне скоупа):
  `Clear()` коллекции `Levels` при `StateChanged` сбрасывает программно установленный
  `SelectedLevel` — предложено отдельной карточкой при необходимости.

## Как проверить

```
dotnet test src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj --nologo -v q
```
Ожидание: 108/108 пройдено (критерий ≥74).