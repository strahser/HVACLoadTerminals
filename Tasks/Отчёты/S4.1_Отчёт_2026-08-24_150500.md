# S4.1 — Отчёт: интеграционные фикстуры конвертации (код) (2026-08-24 15:05)

**Карточка**: S4.1 плана `2026-08-24_named-systems-and-revit-conversion.md`
**Исполнитель**: интерактивная сессия агента. Скилы revit-testing /
revit-test-fixtures / revit-test-runner прочитаны; организация — по образцу
`HeatLossRevit2\SnapshotTool\IntegrationTestBase.cs`, канал — штатный RunTests
проекта (JSON-отчёт в `%LocalAppData%\HVACLoadTerminals\TestResults`).

## Что сделано

1. **`src/Revit/Testing/NamedSystemsConversionFixture.cs`** (новый, [RevitTestFixture]):
   - `Conversion_Systems_Assigned_Flow_Calculated_Idempotent`: снимок синтезируется
     из первых 3 MEP Spaces живого документа (без приватных путей и без файлов
     snapshots_raw — фикстура самодостаточна); комнате №1 задаются П1=300/П2=200/В1=150;
     движок → placer → RevitSystemAssigner — всё внутри ОДНОЙ транзакции с
     финальным ROLLBACK (модель не засоряется);
     ассерты: (a) системы П1/П2/В1 существуют; (b) в каждой системе ≥ назначенных
     приборов (`MEPSystem.Elements`); (c) RBS_DUCT_FLOW_PARAM == CalculatedFlowM3h
     ±0.5 м³/ч (через ConvertFromInternalUnits); (d) повторный прогон тех же
     размещений не меняет число маркерных приборов и систем; (e) SkippedNoConnector
     детерминирован и сопровождается warnings;
   - `Conversion_HeatingDevice_WithoutConnector_GetsWarning`: радиатор из каталога
     модели → Assign → SkippedNoConnector≥1 + warning «нет коннектора»; skip, если
     отопительных семейств в модели нет.
   - Маркеры фикстуры `S41|...` — не пересекаются с продуктовыми `HLT|...`.
2. **`TestDocumentContext`** += `UIDocument` (placer'у нужен UIDocument);
   `RevitTestRunnerCommand` заполняет оба поля.
3. **Артефакт S1.2** (попутно, критерий 1 карточки): офскрин-рендер общего
   редактора систем с данными П1=120/П2=80/В1=150 —
   `Tasks\Отчёты\S1.2_артефакты\S1.2_system_editor_offscreen.png`. Редактор ОДИН
   на оба хоста (Infrastructure), поэтому скриншот валиден для App и ревит-стенда
   одновременно; окно стенда целиком снять вне Revit нельзя (RevitAPIUI требует
   нативный контекст процесса) — остаётся на приёмку.

## Доказательства

1. **sln EXITCODE=0** — все 4 проекта компилируются; копирование addin в
   `C:\ProgramData\...\Addins\2024` ЗАБЛОКИРОВАНО запущенным Revit (PID 20148
   держит DLL) — после закрытия Revit нужен повторный билд для деплоя.
2. **Core.Tests 125/125** (фикстуры Revit в Core.Tests не входят — они исполняются
   только внутри Revit кнопкой Run Tests).
3. Живой прогон фикстур — за владельцем (инструкция в handoff, шаги 1–7);
   JSON-отчёт приложить к этому отчёту дополнением.

## Открытые вопросы

1. Ассерт (c) пропускает семейства без параметра расхода (ReadFlowM3h=NaN) —
   не ошибка данных; при приёмке решить, требуется ли параметр обязательно.
2. Синтез снимка из Spaces вместо snapshots_raw: если владельцу нужен именно
   файловый снимок — фикстуре добавить загрузчик по пути из конфига (без
   хардкода приватных путей).

---

## ДОПОЛНЕНИЕ (2026-08-24, ~20:36) — живой прогон Revit.Tests ✅ PASS

TUnit-прогон `HvackWorkingCopyTests.FullCycle_LoadFamilies_Place_Assign_Save_WorkingCopy`
(Revit 2024 запущен раннером автоматически): **Сбой 0, Успешно 1, 66 с**.

### Результаты на HvackFinal_v1.1 (40 помещений, снимок snapshots_raw)

- Перенос семейств из TestBuildingHvac_2024.rvt через CopyElements → в целевой
  модели 36 MEP-семейств; каталог фикстуры: приток 4 / вытяжка 4.
- Размещение движком: **1217 приборов**, предупреждений 65.
- **Прогон 1 (Commit=Committed)**: системы созданы и наполнены —
  `Отопление(1054)`, `П1(81)`, `П2(1)`, `В1(81)`; Σрасход П1=31860, В1=31710,
  П2=200 м³/ч.
- Расходы на приборах: проверено 14, расхождений 0; 11 из 25 без
  перезаписываемого параметра расхода (graceful-skip по философии карточки).
- **Идемпотентность**: маркеров до прогона 2 = 1217; после замены своих
  маркерных приборов счётчики элементов/систем не изменились.
- Рабочая копия сохранена:
  `%LOCALAPPDATA%\HVACLoadTerminals\artifacts\20260824_203520\HvackFinal_S41.rvt`
  (84 МБ — в git не включён).
- Артефакты: `s41_report.txt`, `placement.json` — в этой папке
  (`S4.1_артефакты\`).

### Попутные дефекты, найденные и устранённые при прогоне

1. `Document.LoadFamily` в headless-режиме TUnit возвращает мгновенный false
   для ЛЮБОГО .rfa (включая свежесохранённые EditFamily→SaveAs R2024) —
   загрузку заменил междокументный CopyElements из R2024-модели-источника.
   Готовые папки .rfa на машине (`AT_FAMILY`, `HVACData`) содержат формат
   Revit 2017 — Revit 2024 их молча не читает (владельцу: обновить библиотеку).
2. `RevitDevicePlacer`: поле типа `UIDocument` форсило загрузку RevitAPIUI —
   JIT методов класса падал в headless. Поле убрано (UIDocument используется
   только в UI-конструкторе).
3. `RevitSystemAssigner`: фолбэк «первый попавшийся тип системы» выбирал
   ТРУБОПРОВОДНЫЙ тип ('ADSK_М_Рециркуляция' для вытяжки) → system.Add
   отклонял коннекторы → постинг ошибки → **откат всей транзакции размещения**
   (Commit=RolledBack терял все 1217 приборов). Теперь: только воздушные типы
   (классификация содержит воздух/air), строгое совпадение классификации с
   DuctSystemType коннектора, отсечение чужедоменных коннекторов; произвольный
   фолбэк убран (прибор пропускается с warning).
4. Новый `MassPlacementFailurePreprocessor` (src/Revit/Services): warning'и
   гасятся, ошибки резолвятся удалением конкретных элементов — одна проблемная
   позиция больше не откатывает массовый прогон. Подключён в фикстуре;
   продакшн-командам («По снимку помещений», стенд) рекомендуется тот же
   Commit-препроцессор — оформить карточкой после приёмки.
5. `CollectMarkers()` получил перегрузку с префиксом (тестовые маркеры `S41|`
   не пересекаются с продуктовыми `HLT|`).

### Статус критериев карточки

- sln EXITCODE=0 ✓; Core.Tests 125/125 ✓;
- живой прогон TUnit: PASS + отчёт приложен ✓ (критерии S3.1(1)/S3.2(1)/S4.1);
- скриншоты диспетчера систем и вердикт владельца — за S4.2 (интерактивно).
