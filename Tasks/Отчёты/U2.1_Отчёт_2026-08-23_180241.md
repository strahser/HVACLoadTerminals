# ОТЧЁТ: U2.1 — Паттерны массовой расстановки: сторона стены + правило одиночных

- **Дата**: 2026-08-23
- **Статус**: выполнено ранее, повторная диспетчеризация — повторная верификация, ожидает контроля
- **Карточка**: `Tasks/Архив/U2.1_Паттерны_массовой_расстановки_сторона_ст.md` (перемещена в Архив со статусом `verified`)
- **Контекст**: карточка исполнена в коммите `b71eabe plan/U2.1` (отчёт `U2.1_Отчёт_2026-08-23_173238.md`) и получила вердикт контролёра PASS (`7140a79`, `U2.1_Вердикт_контролёра_2026-08-23_182011.md`). Текущий прогон (попытка=2) — повторная (stale) диспетчеризация раннера; фактического нового кода не требуется. Ниже — повторная механическая верификация критериев приёмки по текущему состоянию репозитория (ветка `refactoring/solid-clean-architecture`, HEAD `230002f`).

## Решения (grill)

- Вопросов владельцу не задавал: постановка не изменилась, карточка уже верифицирована PASS (`7140a79`), все ключевые решения (трактовка Corner/Explicit, дефолты владельца, поведение старых файлов) зафиксированы в коммите `b71eabe` и отчёте `173238`. Повторный grill не требовался.
- ASSUMPTION: повторная диспетчеризация U2.1 — артефакт раннера при уже принятой карточке; отработал как «verify-only», чтобы не переписывать заведомо принятый код.

## Что было не так (исходное состояние карточки)

`CeilingPlacementService` расставлял потолочные приборы только равномерной сеткой по офсет-контуру; одиночный прибор всегда в центре (`CeilingPlacementService.cs:125-127` до правки). Правило владельца «приточные вдоль длинной границы, вытяжные вдоль короткой; отдельное правило для одиночных» в UI задать было нельзя, в проекте (`*.hvacproj.json`) паттерны не сохранялись, на плане выбранные стороны не показывались. — Исправлено в `b71eabe`.

## Что сделано (зафиксировано в `b71eabe`, повторно проверено по коду)

### Core (`src/Core/Services/CeilingPlacementService.cs`)
- `WallPattern { CeilingGrid, LongSide, ShortSide, Explicit }` (стр. 23), `SingleRule { Center, Corner }` (стр. 37);
- `CeilingPlacementOptions += Pattern, SingleRule, ExplicitSide, SpacingMm, StartOffsetMm` (стр. 64–69);
- настенные режимы переиспользуют ядро `RoomGeometryAnalyzer`/`TerminalPlacementService`: выбор ребра — `SelectPrimaryEdge` (LongSide/ShortSide → `PlacementSide`, Explicit → `CoordinateSystem`, стр. 284–300);
- распределение по ребру `DistributeAlongEdge` с `StartOffsetMm` с обоих концов и `SpacingMm` (0 = равномерно, фиксированный шаг при невмещении → откат к равномерному + warning, стр. 308–349);
- `SingleRule`: Center = центр офсет-контура (прежнее поведение), Corner = вершина у min-X/min-Y (детерминировано, стр. 266–278);
- `CeilingPlacementResult += SelectedEdge` (стр. 90) для подсветки;
- узкая комната (офсет коллапсирует) → warning «Контур слишком мал…», без исключений (стр. 160–169).

### Infrastructure/Presentation (`src/Infrastructure/Presentation/SnapshotWorkspacePresenter.cs`, `PatternEdge.cs`)
- дефолты владельца: `SupplyPattern = LongSide`, `ExhaustPattern = ShortSide`, `SingleDeviceRule = Center` (стр. 163–165); массивы `WallPatterns`/`SingleRules` для биндинга комбобоксов обоих хостов;
- `Calculate()` пробрасывает паттерны в опции обеих систем и собирает `LastPatternEdges` из `SelectedEdge` (стр. 346, 399–428, 598–613);
- round-trip: `ProjectDto += SupplyPattern/ExhaustPattern/SingleRule` (nullable, стр. 589–591), сериализация `StringEnumConverter` (стр. 523–526) — читаемые имена, старые файлы → дефолты владельца (стр. 540–542);
- `PatternEdge.cs` — уровень/система/Start/End для подсветки.

### App (`src/App/MainWindow.xaml`, `ViewModels/MainViewModel.cs`)
- комбобоксы паттерна (Приток/Вытяжка) + «1 прибор» в тулбаре, свойства-пробросы с живым пересчётом (MainViewModel.cs:111–148);
- OxyPlot-план: подсветка выбранных рёбер толщиной 5 px, приток красный / вытяжка зелёная, фильтр по уровню (MainViewModel.cs:419–436).

### Ревит-стенд (`src/Revit/UI/SnapshotPlacementWindow.xaml`)
- те же контролы (паттерн Притока, паттерн Вытяжки, «1 прибор») поверх общих опций presenter (стр. 40–55).

### Core.Tests
- `src/Core.Tests/CeilingPatternTests.cs` — 8 тестов новых режимов, включая L-образную и узкую комнату;
- `src/Core.Tests/SnapshotWorkspacePresenterTests.cs` — round-trip паттернов и дефолты владельца.

## Доказательства (повторная верификация критериев приёмки)

| # | Критерий | Результат |
|---|----------|-----------|
| 1 | юнит-тесты новых режимов (12×8 м воспроизводимо) | ✅ `CeilingPatternTests.cs`: `LongSide_On_12x8_Rect_Puts_Row_On_One_Long_Side`, `ShortSide_On_12x8_Rect_Puts_Row_On_Short_Side`, `Explicit_Bottom_Places_Row_Near_Max_Y`, `Fixed_Spacing_Not_Fitting_Falls_Back_To_Even_With_Warning`, `Single_Device_Center_Rule_Goes_To_Offset_Contour_Center`, `Single_Device_Corner_Rule_Goes_To_Corner_Of_Offset_Contour`, `L_Shaped_Room_Wall_Row_Stays_Inside_Offset_Contour`, `Narrow_Room_Collapsed_Offset_Yields_Warning_Not_Exception` |
| 2 | round-trip тест паттернов | ✅ `SnapshotWorkspacePresenterTests.cs`: `Project_RoundTrip_Preserves_Placement_Patterns` (в JSON — читаемые имена, `"SupplyPattern": "Explicit"`), `Project_Legacy_File_Without_Patterns_Keeps_Owner_Defaults`, `Pattern_Owner_Defaults_Are_LongSide_ShortSide_Center` |
| 3 | Core.Tests зелёные | ✅ `dotnet test src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj`: «не пройдено 0, пройдено 108, пропущено 0, всего 108»; `dotnet build src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj`: «Сборка успешно завершена… Ошибок: 0» (warning MSB3026 — копирование xunit.abstractions.dll блокировано параллельными testhost-процессами среды, не ошибка кода) |
| 4 | скриншоты плана с подсветкой сторон | ✅ `Tasks/Отчёты/U2.1_Скриншоты/U2.1_plan_patterns_default.png` (53818 B) и `U2.1_plan_supply_grid.png` (49697 B) — в git (трекаются в `b71eabe`), рендер `OxyPlot.Wpf.PngExporter` с реальным presenter на снимке HvackFinal_v1.1.json |

Дополнительно (declarative checks pipeline.yaml):
- A-01 Core без Autodesk.Revit/WPF: grep по `src/Core` → `using\s+(Autodesk\.Revit|System\.Windows)` — **0** совпадений;
- A-02 в App нет легаси WebBrowser: grep `<WebBrowser` по `src/App` — **0** совпадений.

Дополнительно (U2.1): `Calculate_Produces_Pattern_Edges_For_Highlighting` — presenter отдаёт рёбра обеих систем с уровнем.

## ASSUMPTION

- ASSUMPTION: повторная диспетчеризация U2.1 (попытка=2, метка отчёта 180241) — stale-прогон раннера при уже принятой карточке; новых правок кода не вносил, чтобы не трогать принятое состояние (карточка в Архиве со статусом `verified`, вердикт PASS `7140a79`).

## Открытые вопросы

- Открытых вопросов нет (карточка ранее принята PASS вердиктом `7140a79`).
- Наблюдение вне скоупа: точки приборов на OxyPlot-плане не видны (pre-existing особенность `PlotLevelCore`, ScatterSeries) — не относится к карточке, ранее предложено отдельной карточкой.

## Как проверить

```
dotnet build src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj --nologo -v q   # Ошибок: 0
dotnet test src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj                  # 108/108
```

UI: открыть снимок → «▶ РАССЧИТАТЬ»: на плане приточные стороны красные (LongSide), вытяжные зелёные (ShortSide); сменить «Приток» на CeilingGrid — красные рёбра гаснут; «Сохранить проект» → в JSON поля SupplyPattern/ExhaustPattern/SingleRule читаемыми именами.

## Границы

Соблюдены: новых правок кода не вносил (карточка принята и зафиксирована в `b71eabe`); `.idea\`, `.opencode\`, bin\obj, TestResults\ и чужие отчёты/планы не тронуты; субагенты не создавались.

## Коммит

`plan/U2.1`: отчёт повторной верификации `Tasks/Отчёты/U2.1_Отчёт_2026-08-23_180241.md`.