# ОТЧЁТ: U2.1 — Паттерны массовой расстановки: сторона стены + правило одиночных

- **Дата**: 2026-08-23
- **Статус**: выполнено ранее, повторная диспетчеризация — повторная верификация, ожидает контроля
- **Карточка**: `Tasks/Архив/U2.1_Паттерны_массовой_расстановки_сторона_ст.md` (перемещена в Архив со статусом `verified`)
- **Контекст**: карточка уже исполнена в коммите `b71eabe plan/U2.1` (отчёт `U2.1_Отчёт_2026-08-23_173238.md`) и получила вердикт контролёра PASS (`7140a79`, `U2.1_Вердикт_контролёра_2026-08-23_182011.md`). Текущий прогон — повторная (stale) диспетчеризация; фактического нового кода не требуется. Ниже — повторная механическая верификация критериев приёмки по текущему состоянию репозитория.

## Решения (grill)

- Вопросов владельцу не задавал: карточка исполнена и верифицирована PASS ранее, все решения зафиксированы в коммите `b71eabe` и вердикте `7140a79`. Повторный grill не требовался — постановка не изменилась.
- ASSUMPTION: повторная диспетчеризация U2.1 (попытка=1, метка времени отчёта 180840) — это артефакт раннера при уже выполненной и приёмной карточке; отработал как «verify-only», чтобы не переписывать заведомо принятый код.

## Что было не так (исходное состояние карточки)

`CeilingPlacementService` расставлял потолочные приборы только равномерной сеткой по офсет-контуру; одиночный прибор всегда в центре (`CeilingPlacementService.cs:125-127` до правки). Правило владельца «приточные вдоль длинной границы, вытяжные вдоль короткой; отдельное правило для одиночных» в UI задать было нельзя, в проекте (`*.hvacproj.json`) паттерны не сохранялись, на плане выбранные стороны не показывались. — Исправлено в `b71eabe`.

## Что сделано (зафиксировано в `b71eabe`, повторно проверено)

### Core (`src/Core/Services/CeilingPlacementService.cs`)
- `WallPattern { CeilingGrid, LongSide, ShortSide, Explicit }` (стр. 23), `SingleRule { Center, Corner }` (стр. 37);
- `CeilingPlacementOptions += Pattern, SingleRule, ExplicitSide, SpacingMm, StartOffsetMm` (стр. 64–69);
- настенные режимы переиспользуют ядро `RoomGeometryAnalyzer`/`TerminalPlacementService` (`SelectPrimaryEdge` → `SidePreference`/`CoordinateSystem`), распределение по ребру с `StartOffsetMm`/`SpacingMm` и откатом при невмещении;
- `SingleRule`: Center = центр офсет-контура, Corner = вершина у min-X/min-Y (стр. 262–283);
- `CeilingPlacementResult += SelectedEdge` (стр. 90) для подсветки;
- узкая комната (коллапс офсета) → warning, без исключений.

### Infrastructure/Presentation (`src/Infrastructure/Presentation/SnapshotWorkspacePresenter.cs`, `PatternEdge.cs`)
- дефолты владельца: `SupplyPattern = LongSide`, `ExhaustPattern = ShortSide`, `SingleDeviceRule = Center` (стр. 163–165); массивы `WallPatterns`/`SingleRules` для биндинга комбобоксов обоих хостов;
- `Calculate()` пробрасывает паттерны в обе системы и собирает `LastPatternEdges` из `SelectedEdge`;
- round-trip: `ProjectDto += SupplyPattern/ExhaustPattern/SingleRule` (nullable, стр. 589–591), сериализация `StringEnumConverter` — читаемые имена, старые файлы → дефолты владельца.

### App (`src/App/MainWindow.xaml`, `MainViewModel.cs`)
- комбобоксы паттерна + «1 прибор» для Притока и Вытяжки, живой пересчёт;
- OxyPlot-план: подсветка выбранных рёбер (5 px, приток красный / вытяжка зелёная).

### Ревит-стенд (`src/Revit/UI/SnapshotPlacementWindow.xaml`)
- те же контролы поверх общих опций presenter; добавлен комбобокс правила количества Вытяжки.

### Core.Tests
- `src/Core.Tests/CeilingPatternTests.cs` — 8 тестов новых режимов (стр. 47–201), включая L-образную и узкую комнату;
- `src/Core.Tests/SnapshotWorkspacePresenterTests.cs` — round-trip паттернов (стр. 133–165).

## Доказательства (повторная верификация критериев приёмки)

| # | Критерий | Результат |
|---|----------|-----------|
| 1 | юнит-тесты новых режимов (12×8 м воспроизводимо) | ✅ `CeilingPatternTests.cs`: `LongSide_On_12x8_Rect_Puts_Row_On_One_Long_Side`, `ShortSide_On_12x8_Rect_Puts_Row_On_Short_Side`, `Explicit_Bottom_Places_Row_Near_Max_Y`, `Fixed_Spacing_Not_Fitting_Falls_Back_To_Even_With_Warning`, `Single_Device_Center_Rule_Goes_To_Offset_Contour_Center`, `Single_Device_Corner_Rule_Goes_To_Corner_Of_Offset_Contour`, `L_Shaped_Room_Wall_Row_Stays_Inside_Offset_Contour`, `Narrow_Room_Collapsed_Offset_Yields_Warning_Not_Exception` |
| 2 | round-trip тест паттернов | ✅ `SnapshotWorkspacePresenterTests.cs`: `Project_RoundTrip_Preserves_Placement_Patterns`, `Project_Legacy_File_Without_Patterns_Keeps_Owner_Defaults`, `Pattern_Owner_Defaults_Are_LongSide_ShortSide_Center` |
| 3 | Core.Tests зелёные | ✅ `dotnet test src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj`: «не пройдено 0, пройдено 108, пропущено 0, всего 108»; `dotnet build HVACLoadTerminals.sln`: «Сборка успешно завершена. Предупреждений: 0, Ошибок: 0» (второй прогон; первый разово споткнулся о временную блокировку `HVACLoadTerminals.Revit_MarkupCompile.cache` параллельными сборками среды — не ошибка кода) |
| 4 | скриншоты плана с подсветкой сторон | ✅ `Tasks/Отчёты/U2.1_Скриншоты/U2.1_plan_patterns_default.png` (53818 B) и `U2.1_plan_supply_grid.png` (49697 B) в репозитории |

Дополнительно: `Calculate_Produces_Pattern_Edges_For_Highlighting` (presenter отдаёт рёбра обеих систем с уровнем).

## ASSUMPTION

- ASSUMPTION: повторная диспетчеризация U2.1 — stale-прогон раннера при уже принятой карточке; новых правок кода не вносил, чтобы не трогать принятое состояние (карточка в Архиве со статусом `verified`).

## Открытые вопросы

- Открытых вопросов нет (карточка ранее принята PASS вердиктом `7140a79`).
- Наблюдение вне скоупа: точки приборов на OxyPlot-плане не видны (pre-existing особенность `PlotLevelCore`), ранее предложено отдельной карточкой.

## Как проверить

```
dotnet build HVACLoadTerminals.sln -v q        # Сборка успешно завершена, Ошибок: 0
dotnet test src\Core.Tests\HVACLoadTerminals.Core.Tests.csproj   # 108/108
```

UI: открыть снимок → «▶ РАССЧИТАТЬ»: на плане приточные стороны красные (LongSide), вытяжные зелёные (ShortSide); сменить «Приток» на CeilingGrid — красные рёбра гаснут; «Сохранить проект» → в JSON поля SupplyPattern/ExhaustPattern/SingleRule читаемыми именами.

## Границы

Соблюдены: новых правок кода не вносил (карточка принята); `.idea\`, `.opencode\`, bin\obj, TestResults\ и чужие отчёты/планы не тронуты; субагенты не создавались.

## Коммит

`plan/U2.1`: отчёт повторной верификации `Tasks/Отчёты/U2.1_Отчёт_2026-08-23_180840.md`.