<!-- TeeConfigurationWindow.xaml -->
<Window x:Class="HVACLoadTerminals.PipeSewageHandler.TeeConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка тройника" Height="450" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Выбор семейства -->
        <ComboBox x:Name="FamilyComboBox" Grid.Row="0"
                  DisplayMemberPath="FamilyName"
                  Margin="0,0,0,10"/>

        <!-- Смещение -->
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Смещение (мм):" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="OffsetTextBox" Width="100" Text="200"/>
        </StackPanel>

        <!-- Параметры -->
        <DataGrid x:Name="ParametersGrid" Grid.Row="2"
                  AutoGenerateColumns="False"
                  Margin="0,10">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Выбрать" Binding="{Binding IsSelected}" Width="Auto"/>
                <DataGridTextColumn Header="Параметр" Binding="{Binding Name}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Значение" Binding="{Binding Value}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="Применить" Click="Apply_Click" Width="80" HorizontalAlignment="Right"/>
    </Grid>
</Window>

