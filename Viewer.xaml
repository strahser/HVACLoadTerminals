<Window x:Class="HVACLoadTerminals.Viewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:HVACLoadTerminals"
             xmlns:viewModel="clr-namespace:HVACLoadTerminals.ViewModel" 
        d:DesignHeight="450" d:DesignWidth="800">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>       
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0" Grid.Row="0">
            <TextBlock>Category Choose</TextBlock>
            <ComboBox x:Name="CategorySelectedLabelComboBox" ItemsSource="{Binding Categoryes.Keys}" SelectedItem="{Binding SelectedCategory}" SelectedIndex="{Binding Categoryes.Keys}" />
            <TextBlock Text="{Binding SelectedCategory}"></TextBlock>
        </StackPanel>
    </Grid>
</Window>
