<Window x:Class="HVACLoadTerminals.HeatLoss.DrawNewSpaceFaces.FloorsRoofs.DrawFloorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создание перекрытий/кровли" Height="300" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        FontFamily="Segoe UI"
        FontSize="14">

   <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HVACLoadTerminals;component/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Создание перекрытий и кровель"
                   Grid.Row="0"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>
        
        <!-- Уровень для фильтрации пространств -->
        <Label Content="Уровень пространств:" Grid.Row="2"/>
        <ComboBox Name="SpaceLevelComboBox" ItemsSource="{Binding Levels}"
                  DisplayMemberPath="Name" Grid.Row="2"
                  SelectedItem="{Binding SpaceLevel}"/>

        <!-- Уровень для отрисовки -->
        <Label Content="Уровень для отрисовки:" Grid.Row="1"/>
        <ComboBox Name="LevelComboBox" ItemsSource="{Binding Levels}"
                  DisplayMemberPath="Name" Grid.Row="1"
                  SelectedItem="{Binding SelectedLevel}"/>
        
        <!-- Выбор пространств -->
        <!--<Button Content="Выбрать пространства"
                Grid.Row="3"
                Margin="0,20,0,10"
                Command="{Binding PickSpacesCommand}"
                ToolTip="Выберите пространства в модели Revit"/>-->

        <!-- Команды отрисовки -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Grid.Row="4"
                    Margin="0,20,0,10">
            <Button Content="Создать перекрытия"
                    Command="{Binding DrawFloorCommand}"
                    Width="150"
                    Margin="5"
                    Style="{StaticResource DrawFloorsButtonStyle}"
                    ToolTip="Создать перекрытия для выбранных пространств"/>

            <Button Content="Создать кровли"
                    Command="{Binding DrawRoofCommand}"
                    Width="150"
                    Margin="5"
                    Style="{StaticResource DrawRoofsButtonStyle}"
                    ToolTip="Создать кровли для выбранных пространств"/>
        </StackPanel>
    </Grid>
</Window>